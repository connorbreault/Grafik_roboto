// === MATERIALIZE FUNCTIONS === //
$(document).ready(function () {
    M.AutoInit();
    // $(".sidenav").sidenav()
    // $(".modal").modal()
    // $('.parallax').parallax();
    // $('.collapsible').collapsible();
    // $('.materialboxed').materialbox();
    // $('.carousel').carousel();
});